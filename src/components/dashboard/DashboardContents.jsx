const EnemiesContent = () => {
    // TODO: load in the content from a database

    return (
        <div>
            Yeah yoh this will load the enemies (tasks) from the database
        </div>
    )
}

const TrophiesContent = () => {
    // TODO: load in the content from a database

    return (
        <div>
            Yeah yoh this will load the Trophies (finished tasks) from the database
        </div>
    )
}

const JourneyContent = () => {
    // TODO: load in the content from a database

    return (
        <div>
            Yeah yoh this will load the journey maybe a graph? or timeline? idk basta visualiazation han past activities like frequency of task completions or what-not
        </div>
    )
}

export { EnemiesContent, TrophiesContent, JourneyContent }

// Consider adding a mature mode where trophies are...quite something
